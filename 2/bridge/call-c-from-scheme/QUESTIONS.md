- how does memory management work with chicken scheme & a foreign-lambda
  that returns a malloc'd string?
- instead of a binary, can csc generate a .c file that I can compile (with -g
  to check with LLDB)
